name: register_page
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0" # Supports Dart 2.12+ for null safety.

dependencies:
  flutter:
    sdk: flutter
  flutter_dropdown: ^1.0.0
  http: ^1.2.2
  tensorflow_lite_flutter: ^2.0.0
  image_picker: ^0.8.8+5
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  image: ^3.0.1
  path_provider: ^2.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
